[project]
name = "project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "cython>=3.2.4",
    "pandas>=3.0.2",
    "pypdf>=6.10.2",
    "pytest>=9.0.3",
]

[project.scripts]
toolkit = "toolkit.cli:main"
